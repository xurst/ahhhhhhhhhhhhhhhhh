1. add clipboard fetch to the keyboard logger

2. upgrade the penetration system where when running it, it wiill open a terminal with a menu with the options bitlocker check start keylogger boot into windows recovery and exit. boot into windows recovery will boot u into the windows blue screen. start keylogger will start the logger. bitlocker check will check for bitlocker. and also another option (change admin password and login (used for network domains) where that will attempt to do what it says via the help of the windows boot recovery screen and all that and also add a option where u can do the actual file renaming for the cmd on lock screen to work (may have to adjust a like because how would u execute usb code inside of windows boot recovery screen or on the lock screen?) addition ot the bitlocker check, it will also log the result into the computer folder where the script is being ran from. finish today so u can use by friday. and also add another option named Main Penetrator where it will run the main code from the previous discord rat

3. make a complex system where the usb will have its own dir system where when plugged in u can choose to fetch its file sources and for example if plugging into a teacher pc for the first time, u can have the option to inject everything into a folder on the c drive. and if injecting for another time u can have the option to fetch those files and put them on the usb

4. the primary way of injection would be the keylogger add a option to double click me bat where there are a couple options like bitlocker check start keylogger get keylogger results and a nother option that would pc information